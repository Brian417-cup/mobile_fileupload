const web_header = 'http://';
const serveice_ip = '192.168.43.244';
const service_port = '82';
const image_upload_page = 'image_upload';

//键值对
const servicePath = {
  image_upload_page:
      '${web_header}${serveice_ip}:${service_port}/${image_upload_page}',
};
